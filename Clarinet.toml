[project]
name = "STX-Reputation-Scoring"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/STX-Reputation-Scoring/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.STX-Reputation-Scoring]
path = "contracts/STX-Reputation-Scoring.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
